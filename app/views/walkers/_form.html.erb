<%= form_with(model: walker) do |form| %>
  <% if walker.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(walker.errors.count, "error") %> prohibited this walker from being saved:</h2>

      <ul>
        <% walker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :terms %>
    <%= form.check_box :terms %>
  </div>

  <div class="field">
    <%= form.label :zip %>
    <%= form.number_field :zip %>
  </div>

  <div class="field">
    <%= form.label :range %>
    <%= form.number_field :range %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
