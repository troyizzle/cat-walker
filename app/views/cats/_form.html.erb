<%= form_for(cat) do |f| %>
  Picture???
  <%= f.tw_text_field :name, { autofocus: true, placeholder: 'Name', class: 'w-full' } %>
  <%= f.tw_text_field :breed, { placeholder: 'Breed', class: 'w-full' } %>
  <%= f.tw_text_field :weight, { placeholder: 'Weight', class: 'w-fulll' } %>
<% end %>
